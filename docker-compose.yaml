services:
  spark-jupyter:
    image: jupyter/pyspark-notebook
    container_name: data-processer-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./:/home/jovyan/work/
    environment:
      - JUPYTER_ENABLE_LAB=yes
    restart: always